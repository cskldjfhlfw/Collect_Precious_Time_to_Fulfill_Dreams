# =============================================================================
# 环境变量配置模板
# 请复制此文件为 .env 并填写实际配置
# 命令: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# JWT 密钥配置（⭐ 必须设置）
# -----------------------------------------------------------------------------
# 警告：这是最重要的安全配置！
# 为什么必须自己配置？
# 1. 安全性：每个项目必须有独特的密钥，不能使用默认值
# 2. 不可逆：一旦生产环境使用，不能随意更改（会导致所有 Token 失效）
# 3. 隐私性：绝对不能提交到版本控制系统
#
# 生成方法：
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# 或
# python -c "import secrets; print(secrets.token_urlsafe(64))"  # 更强的密钥
#
APP_JWT_SECRET_KEY=

# -----------------------------------------------------------------------------
# PostgreSQL 数据库配置
# -----------------------------------------------------------------------------
APP_POSTGRES_ENABLED=true
APP_POSTGRES_DSN=postgresql+asyncpg://user:password@localhost:5432/academic_db
# 示例: postgresql+asyncpg://postgres:mypassword@localhost:5432/academic_system

# -----------------------------------------------------------------------------
# Neo4j 图数据库配置（知识图谱）
# -----------------------------------------------------------------------------
APP_NEO4J_ENABLED=true
APP_NEO4J_URI=bolt://localhost:7687
APP_NEO4J_USER=neo4j
APP_NEO4J_PASSWORD=your-neo4j-password
APP_NEO4J_DATABASE=neo4j

# -----------------------------------------------------------------------------
# MongoDB 配置（文档存储）
# -----------------------------------------------------------------------------
APP_MONGO_ENABLED=true
APP_MONGO_DSN=mongodb://localhost:27017
APP_MONGO_DATABASE=academic_db
# 如果 MongoDB 启用了认证：
# APP_MONGO_DSN=mongodb://username:password@localhost:27017

# -----------------------------------------------------------------------------
# Redis 配置（缓存和 Token 黑名单）
# -----------------------------------------------------------------------------
APP_REDIS_ENABLED=true
APP_REDIS_DSN=redis://localhost:6379/0
# 如果 Redis 设置了密码：
# APP_REDIS_DSN=redis://:password@localhost:6379/0
APP_REDIS_SSL=false

# -----------------------------------------------------------------------------
# AI 大模型配置（可选）
# -----------------------------------------------------------------------------
# 智谱 AI 配置
# 获取地址: https://open.bigmodel.cn/
APP_ZHIPU_API_KEY=

# OpenAI 配置（备选）
APP_OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# 应用配置
# -----------------------------------------------------------------------------
# 应用名称
APP_APP_NAME=Academic Management System

# 运行环境: development, production, testing
APP_ENVIRONMENT=development

# API 路由前缀
APP_API_PREFIX=/api

# CORS 跨域配置（前端地址）
APP_CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# -----------------------------------------------------------------------------
# 项目启动配置
# -----------------------------------------------------------------------------
# 项目启动默认时长（小时）
APP_PROJECT_STARTUP_DURATION_HOURS=1

# -----------------------------------------------------------------------------
# 其他配置
# -----------------------------------------------------------------------------
# PostgreSQL Echo（开发时可启用，生产环境关闭）
APP_POSTGRES_ECHO=false
